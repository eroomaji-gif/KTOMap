-- ==========================================
-- 📌 [ระบบป้องกัน] รายชื่อเจ้าของสคริปต์
-- ==========================================
local ProtectedOwners = {
    "Onsenb47162",
    "Agakami_5625",
}

local function isOwner(player)
    if not player then return false end
    for _, ownerName in ipairs(ProtectedOwners) do
        if player.Name == ownerName or player.DisplayName == ownerName then
            return true
        end
    end
    return false
end

-- ==========================================
-- 📌 โครงสร้าง UI หลัก
-- ==========================================
local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer
local playerGui = localPlayer:WaitForChild("PlayerGui")

local oldGui = playerGui:FindFirstChild("FlexibleMenuGui")
if oldGui then
    oldGui:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "FlexibleMenuGui"
ScreenGui.Parent = playerGui
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.DisplayOrder = 9999

-- ปุ่มเปิด/ปิดหน้าต่างเมนู
local ToggleButton = Instance.new("ImageButton")
ToggleButton.Name = "ToggleButton"
ToggleButton.Parent = ScreenGui
ToggleButton.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
ToggleButton.BackgroundTransparency = 0.2
ToggleButton.Position = UDim2.new(0.05, 0, 0.1, 0)
ToggleButton.Size = UDim2.new(0, 45, 0, 45)
ToggleButton.Active = true
ToggleButton.Draggable = true
ToggleButton.Image = "rbxassetid://75925823414835"

task.spawn(function()
    pcall(function()
        game:GetService("ContentProvider"):PreloadAsync({ToggleButton})
    end)
end)

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(0, 8)
ToggleCorner.Parent = ToggleButton

-- หน้าต่างหลัก
local MainFrame = Instance.new("ImageLabel")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
MainFrame.BackgroundTransparency = 1
MainFrame.Position = UDim2.new(0.05, 0, 0.18, 0)
MainFrame.Size = UDim2.new(0, 550, 0, 450)
MainFrame.Visible = false
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Image = "rbxassetid://75925823414835"
MainFrame.ImageTransparency = 0.45

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 10)
MainCorner.Parent = MainFrame

-- แถบหัวข้อด้านบน
local TitleBar = Instance.new("TextLabel")
TitleBar.Name = "TitleBar"
TitleBar.Parent = MainFrame
TitleBar.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
TitleBar.Size = UDim2.new(1, 0, 0, 40)
TitleBar.Font = Enum.Font.GothamBold
TitleBar.Text = "  🇹🇭 KTO CRUB USE MAP "
TitleBar.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleBar.TextSize = 15
TitleBar.TextXAlignment = Enum.TextXAlignment.Left

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 10)
TitleCorner.Parent = TitleBar

local CloseButton = Instance.new("TextButton")
CloseButton.Name = "CloseButton"
CloseButton.Parent = TitleBar
CloseButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
CloseButton.Position = UDim2.new(1, -45, 0.5, -15)
CloseButton.Size = UDim2.new(0, 30, 0, 30)
CloseButton.Font = Enum.Font.GothamBold
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.TextSize = 14

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 6)
CloseCorner.Parent = CloseButton

CloseButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
end)

ToggleButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

-- ==========================================
-- 📂 แถบหมวดหมู่ฝั่งซ้าย (Sidebar)
-- ==========================================
local SidebarFrame = Instance.new("ScrollingFrame")
SidebarFrame.Name = "SidebarFrame"
SidebarFrame.Parent = MainFrame
SidebarFrame.BackgroundTransparency = 1
SidebarFrame.Position = UDim2.new(0, 10, 0, 50)
SidebarFrame.Size = UDim2.new(0, 140, 1, -60)
SidebarFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
SidebarFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y
SidebarFrame.ScrollBarThickness = 4

local SidebarLayout = Instance.new("UIListLayout")
SidebarLayout.Parent = SidebarFrame
SidebarLayout.SortOrder = Enum.SortOrder.LayoutOrder
SidebarLayout.Padding = UDim.new(0, 8)

-- ปุ่มหมวดหมู่: เลือกรันแมพ (Game Hub)
local CatBtnMapHub = Instance.new("TextButton")
CatBtnMapHub.Name = "CategoryMapHub"
CatBtnMapHub.Parent = SidebarFrame
CatBtnMapHub.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
CatBtnMapHub.Size = UDim2.new(1, 0, 0, 40)
CatBtnMapHub.Font = Enum.Font.GothamBold
CatBtnMapHub.Text = "🎮 เลือกรันแมพ"
CatBtnMapHub.TextColor3 = Color3.fromRGB(255, 255, 255)
CatBtnMapHub.TextSize = 13

local CatCornerMapHub = Instance.new("UICorner")
CatCornerMapHub.CornerRadius = UDim.new(0, 6)
CatCornerMapHub.Parent = CatBtnMapHub

-- ==========================================
-- 📌 พื้นที่แสดงผลฝั่งขวา (ScrollingFrame + Grid รองรับการเลื่อนยาวๆ)
-- ==========================================
local MapHubContainer = Instance.new("ScrollingFrame")
MapHubContainer.Name = "MapHubContainer"
MapHubContainer.Parent = MainFrame
MapHubContainer.BackgroundTransparency = 1
MapHubContainer.Position = UDim2.new(0, 160, 0, 50)
MapHubContainer.Size = UDim2.new(1, -170, 1, -60)
MapHubContainer.CanvasSize = UDim2.new(0, 0, 0, 0)
MapHubContainer.AutomaticCanvasSize = Enum.AutomaticSize.Y -- ยืดความสูงอัตโนมัติเพื่อให้เลื่อนได้เยอะๆ
MapHubContainer.ScrollBarThickness = 6
MapHubContainer.Visible = true -- เปิดหน้าต่างนี้เป็นค่าเริ่มต้น

local MapGrid = Instance.new("UIGridLayout")
MapGrid.Parent = MapHubContainer
MapGrid.CellSize = UDim2.new(0, 115, 0, 135) -- ขนาดกล่องแต่ละแมพ
MapGrid.CellPadding = UDim2.new(0, 10, 0, 10)
MapGrid.SortOrder = Enum.SortOrder.LayoutOrder



-- ==========================================
-- 📌 [เพิ่ม/แก้ไข รายชื่อแมพและสคริปต์ของคุณตรงนี้ได้เลย]
-- ==========================================
local myGameHubList = {
    {
        name = "The Kill Ler",
        image = "rbxassetid://108021829332546", -- แนะนำใช้ไอดีรูปมาตรฐานที่ผ่านระบบแล้ว
        scriptUrl = "https://raw.githubusercontent.com/eroomaji-gif/Killer/refs/heads/main/Kill_ler"
    },
    {
        name = "Bloxfruit",
        image = "rbxassetid://103350906665199",
        scriptUrl = "https://raw.githubusercontent.com/eroomaji-gif/bloxfruit/refs/heads/main/bloxfruit"
    },
    {
        name = "Mem See",
        image = "rbxassetid://83157689355197",
        scriptUrl = "https://raw.githubusercontent.com/eroomaji-gif/MemSee/refs/heads/main/MemSee"
    },
    {
        name = "แมพที่ 4 (ตัวอย่าง)",
        image = "rbxassetid://6023426915",
        scriptUrl = "https://raw.githubusercontent.com/username/repo/main/script4.lua"
    },
}

-- สร้างปุ่มแมพลงในตาราง Grid แบบอัตโนมัติ
for _, data in ipairs(myGameHubList) do
    local MapCard = Instance.new("TextButton")
    MapCard.Parent = MapHubContainer
    MapCard.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    MapCard.AutoButtonColor = true
    MapCard.Text = ""

    local CardCorner = Instance.new("UICorner")
    CardCorner.CornerRadius = UDim.new(0, 8)
    CardCorner.Parent = MapCard

    -- รูปภาพปกแมพ (เพิ่มระบบป้องกันและบังคับโหลดผ่าน ContentProvider)
    local MapIcon = Instance.new("ImageLabel")
    MapIcon.Parent = MapCard
    MapIcon.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    MapIcon.Position = UDim2.new(0, 5, 0, 5)
    MapIcon.Size = UDim2.new(1, -10, 0, 90)
    MapIcon.Image = data.image
    MapIcon.BackgroundTransparency = 0

    -- สั่งให้ตัวเกมช่วยพรีโหลดภาพล่วงหน้าบังคับดึงข้อมูล
    task.spawn(function()
        pcall(function()
            game:GetService("ContentProvider"):PreloadAsync({MapIcon})
        end)
    end)

    local IconCorner = Instance.new("UICorner")
    IconCorner.CornerRadius = UDim.new(0, 6)
    IconCorner.Parent = MapIcon

    -- ชื่อแมวด้านล่างรูป
    local MapName = Instance.new("TextLabel")
    MapName.Parent = MapCard
    MapName.BackgroundTransparency = 1
    MapName.Position = UDim2.new(0, 0, 0, 97)
    MapName.Size = UDim2.new(1, 0, 0, 32)
    MapName.Font = Enum.Font.GothamBold
    MapName.Text = data.name
    MapName.TextColor3 = Color3.fromRGB(255, 255, 255)
    MapName.TextSize = 12
    MapName.TextWrapped = true

    -- เมื่อกดเลือกแมพ: ลบ UI ทิ้งทั้งหมด แล้วรันสคริปต์ใหม่ทันที
    MapCard.MouseButton1Click:Connect(function()
        print("🚀 [GameHub] กำลังรันสคริปต์และปิด UI เดิม: " .. data.name)
        
        if ScreenGui then
            ScreenGui:Destroy()
        end
        
        pcall(function()
            loadstring(game:HttpGet(data.scriptUrl))()
        end)
    end)
end
